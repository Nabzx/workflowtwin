# API application

The deployable API implementation lives in `src/workflowtwin`. Runtime and container entry points target `workflowtwin.main:app`; this directory documents the application boundary without duplicating Python packages.

